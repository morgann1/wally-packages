[package]
name = "morgann1/shared-toolbar"
description = "Cross-plugin Studio toolbar: multiple plugins share one toolbar via a BindableEvent medium."
version = "0.1.0"
license = "MIT"
authors = ["morgann1"]
registry = "https://github.com/UpliftGames/wally-index"
realm = "shared"
